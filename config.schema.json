{
  "pluginAlias": "EufyRobovacMatter",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "username": {
        "title": "Eufy Username",
        "type": "string",
        "required": true
      },
      "password": {
        "title": "Eufy Password",
        "type": "string",
        "required": true
      },
      "disableMatterStatePush": {
        "title": "Disable Matter state push updates (troubleshooting)",
        "type": "boolean",
        "default": false,
        "description": "Stops plugin-driven state updates to Matter. Useful if pairing logs repeated 'unknown session' messages, but Home status refresh becomes slower."
      }
    }
  }
}
